本文件夹内存放h5类型数据集制作的matlab代码。
主文件为GetPatch_manual_yuv.m
h5为生成的数据集所在文件夹，data_rec为重建点云放置位置。
data_ori为原始点云放置位置，同时放置testFile.txt或trainFile.txt，以便读取所需的点云。
当编码配置为几何有损时，需要先运行reOrder.m文件，输入原始点云与某一码率的重建点云，输出一个新的点云，
使得该生成的点云与重建点云点数相同，且依据顺序，每个点是重建点云中每个点对应的标签。
